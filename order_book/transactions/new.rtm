CALL_FUNCTION
    Address("${ORDER_BOOK_PACKAGE}")
    "OrderBook"
    "new"
    Enum<2u8>(
        Enum<0u8>(
            Enum<0u8>(
                Enum<1u8>(
                    Address("${ADMIN_BADGE}")
                )
            )
        )
    )
    Enum<0u8>()
    Address("${TOKEN_X}")
    Address("${TOKEN_Y}")
    Enum<0u8>()
;
